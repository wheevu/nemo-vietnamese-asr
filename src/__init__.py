"""Workspace-local utilities.

Note: This repository historically used `src/` as a folder name, but did not
mark it as a Python package. Adding this file allows imports like:

    from src.model_utils import load_nemo_model

which keeps the notebook-extracted utilities easy to run as scripts.
"""
